Game state still refreshes when the host refreshes the page.

The main menu is broken. The marigns are too wide and the half the text gets
hidden as a result. It also has too many things. The game package manager
should be its own tab/page. Then, on the page it should list the locally
installed packages, and prompt to make a new one. 

In the menu to make or modify a game package, it separate cards/pieces etc more
clearly, and represent them visually rather than just simply as a list.

Stacks of cards should be able to have labels. On the game package menu, the cards
should be organized as stacks (user configured). e.g, for avalon, a stack of all
the role cards.

The user should also be able to configure, visually, how the table should look
on a new start of the game.

In the game menu, the cards and tokens etc shoul also be pixel art themed
